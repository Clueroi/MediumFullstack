name: fullstack-web

services:
  pg:
    image: bitnami/postgresql:13.16.0
    ports:
      - '3333:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=inorbit